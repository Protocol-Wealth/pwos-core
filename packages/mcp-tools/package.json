{
  "name": "@pwos/mcp-tools",
  "version": "0.1.0",
  "description": "PWOS Core: mcp-tools package",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "peerDependencies": {
    "typescript": "^5.6.0"
  }
}
